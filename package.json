{
  "name": "ekopru-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "build:preview": "node scripts/build.mjs --preview",
    "build:candidate": "node scripts/build.mjs --candidate",
    "check": "node scripts/check.mjs",
    "check:preview": "node scripts/check.mjs dist-preview",
    "check:candidate": "node scripts/check.mjs dist-candidate",
    "check:external": "node scripts/check-external-links.mjs --run",
    "validate": "node scripts/validate.mjs",
    "test": "node --test tests/*.test.mjs",
    "preview": "node scripts/serve.mjs dist"
  },
  "engines": { "node": ">=22" }
}
